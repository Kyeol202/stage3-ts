import { Provider } from 'mobx-react';
import * as React from 'react';

function Store() {

    return (
        
        <Provider 
            
        >
    </Provider>
    );
}
export default Store;